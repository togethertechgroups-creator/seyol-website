'use client';

import React from 'react';
import { BundleBuilder as InteractiveBundleBuilder } from '../interactive/BundleBuilder';

export const BundleBuilder: React.FC = () => {
  return <InteractiveBundleBuilder />;
};
